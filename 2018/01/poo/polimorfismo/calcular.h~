#ifndef CALCULAR_H
#define CALCULAR_H 
class Calcular{
 public:
  int soma(const int &  v1);
  float soma(const float &  v1);
  int soma(const int & v1, const int & v2);
  float soma(const float & v1, const float & v2);
}
#endif
