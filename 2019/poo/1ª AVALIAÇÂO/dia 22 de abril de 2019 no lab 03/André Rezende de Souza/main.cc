#include <stdio.h>
#include <string.h>
#include <cstdlib>
#include <ctime>

void Jogador(nome, overall) {
	return string nome;
	return int overall[10];
}

void calc_eficiencia(overall) {
	int n = srand((unsigned)time(0));
	int ef = n % (100-1+1)+1) * overall;
	return ef;
}

