#include<cstdlib> //para utilizar a função srand que deve ser utilizada sempre antes da fórmula apresentada
#include<ctime>
#include<stdio.h>
int main{
    float 
    overall=0;
    a=0;
    b=0;
    scanf("%f",&overall);
    for(i=0; i>a; i++){
        for(i=0; i>b; i++)
        srand((unsigned)time(0));
        ​res=( rand()%(100-1+1) + 1) * overall;
        <<count "result", res count>>

  }

return 0;}
